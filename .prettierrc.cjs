// .prettierrc.js
module.exports = {
  // 모든 구문의 끝에 세미콜론을 추가할지 여부입니다.
  semi: true,

  // 문자열에 작은따옴표(') 대신 큰따옴표(")를 사용할지 여부입니다. (원래 설정 유지)
  singleQuote: false,

  // 여러 줄로 된 객체나 배열의 마지막 요소 뒤에 후행 쉼표를 추가할지 여부입니다. (원래 설정 유지)
  trailingComma: 'all',

  // 한 줄의 최대 길이입니다. (원래 설정 유지)
  printWidth: 100,

  // 들여쓰기 시 탭 대신 스페이스를 사용할 경우, 스페이스 몇 칸으로 들여쓸지 정의합니다.
  tabWidth: 2,

  // 들여쓰기 시 스페이스 대신 탭 문자를 사용할지 여부입니다. 일반적으로 false로 설정합니다.
  useTabs: false,

  // 줄바꿈 문자 스타일입니다. (원래 설정 유지)
  endOfLine: 'lf',
};
